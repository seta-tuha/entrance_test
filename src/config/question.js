export const levelConfig = ['easy', 'medium', 'hard'];

export const typeConfig = ['single choice', 'multiple choices'];

export const optionName = ['A', 'B', 'C', 'D'];

const config = {
  level: levelConfig,
  type: typeConfig,
  optionName,
}

export default config;
